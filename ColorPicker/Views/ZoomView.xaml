<UserControl x:Class="ColorPicker.Views.ZoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ColorPicker.Views"
             mc:Ignorable="d" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             d:DesignHeight="450" d:DesignWidth="800" BorderBrush="Black" BorderThickness="2" Focusable="False">
    <UserControl.Resources>
        <sys:Int32 x:Key="baseImageSize">50</sys:Int32>
    </UserControl.Resources>
    <Image Source="{Binding ZoomArea}" RenderOptions.BitmapScalingMode="NearestNeighbor" Stretch="Fill">
        <Image.Width>
            <MultiBinding Converter="{StaticResource zoomFactorToSizeConverter}">
                <Binding Source="{StaticResource baseImageSize}" />
                <Binding Path="ZoomFactor"/>
            </MultiBinding>
        </Image.Width>

        <Image.Height>
            <MultiBinding Converter="{StaticResource zoomFactorToSizeConverter}">
                <Binding Source="{StaticResource baseImageSize}" />
                <Binding Path="ZoomFactor"/>
            </MultiBinding>
        </Image.Height>
    </Image>
</UserControl>
